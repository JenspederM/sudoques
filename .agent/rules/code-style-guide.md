---
trigger: always_on
---

This project uses `bun`.

Unit tests are executed with `bun test`, and UI tests with `bun test:ui`.

After making changes run `bun run format` to ensure correct formatting.

Before any commit, all linting errors and warnings should be resolved `bun run lint`

Always ask the user before committing changes. 